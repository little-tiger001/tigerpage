---
import Layout from '../layouts/Layout.astro';

const contactMethods = [
  {
    name: "邮箱",
    value: "little.tiger@example.com",
    icon: "📧"
  },
  {
    name: "GitHub",
    value: "github.com/littletiger",
    icon: "💻"
  },
  {
    name: "LinkedIn",
    value: "linkedin.com/in/littletiger",
    icon: "🔗"
  }
];
---

<Layout title="联系我 - LittleTiger">
  <div class="py-12">
    <h1 class="text-3xl font-bold mb-8">联系我</h1>
    
    <div class="max-w-2xl mx-auto">
      <p class="text-lg mb-10 text-center">
        如果你想与我交流或有合作机会，请随时通过以下方式联系我：
      </p>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
        {contactMethods.map((method) => (
          <div class="bg-gray-800 p-6 rounded-lg text-center">
            <div class="text-3xl mb-3">{method.icon}</div>
            <h3 class="font-semibold mb-2">{method.name}</h3>
            <p class="text-primary">{method.value}</p>
          </div>
        ))}
      </div>
      
      <div class="bg-gray-800 p-8 rounded-lg">
        <h2 class="text-2xl font-bold mb-6 text-center">给我留言</h2>
        <form class="space-y-6">
          <div>
            <label for="name" class="block mb-2">姓名</label>
            <input 
              type="text" 
              id="name" 
              class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary" 
              placeholder="请输入您的姓名"
            />
          </div>
          
          <div>
            <label for="email" class="block mb-2">邮箱</label>
            <input 
              type="email" 
              id="email" 
              class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary" 
              placeholder="请输入您的邮箱"
            />
          </div>
          
          <div>
            <label for="message" class="block mb-2">消息</label>
            <textarea 
              id="message" 
              rows="5" 
              class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary" 
              placeholder="请输入您的消息"
            ></textarea>
          </div>
          
          <div class="text-center">
            <button 
              type="submit" 
              class="btn btn-lg px-8"
            >
              发送消息
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</Layout>